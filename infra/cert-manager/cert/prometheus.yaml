apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: goyo-prometheus-tls
  namespace: traefik
spec:
  secretName: goyo-prometheus-tls
  issuerRef:
    name: goyoai-tools
    kind: ClusterIssuer
  dnsNames:
    - gdpro.goyoai.com
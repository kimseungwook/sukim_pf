apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: tekton-trigger-back-portal-https
  namespace: tekton-pipelines
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`gdwbackportaldev.goyoai.com`)
      priority: 14
      services:
        - name: el-goyoai-web-back-portal
          port: 8080
  tls: 
    secretName: tekton-trigger-back-portal-tls

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tekton-trigger-homepage-tls
  namespace: tekton-pipelines
spec:
  secretName: tekton-trigger-homepage-tls
  issuerRef:
    name: tekton-dev
    kind: ClusterIssuer
  dnsNames:
    - gdwhomepage.goyoai.com
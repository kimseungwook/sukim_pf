apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tekton-trigger-back-portal-scheduler-tls
  namespace: tekton-pipelines
spec:
  secretName: tekton-trigger-back-portal-scheduler-tls
  issuerRef:
    name: tekton-dev
    kind: ClusterIssuer
  dnsNames:
    - gdwbackportalschedulerdev.goyoai.com
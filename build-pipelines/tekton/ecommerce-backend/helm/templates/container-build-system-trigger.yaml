apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: {{ required "A value for .Values.system_build_task_name is required!" .Values.system_build_task_name }}-trigger
  namespace: tekton-pipelines
spec:
  description: >-
    This Task triggers a Kaniko Job on Virtual Node and waits for completion.
  params:
    - name: container-registry-url
      type: string
    - name: container-registry-cache-url
      type: string
    - name: timestamp
      type: string
    - name: project-name
      type: string
      default: {{ .Values.project_name }}
    - name: git-short-sha
      type: string
    - name: module-name
      type: string
      description: "The module to build"
    - name: dockerfile-name
      type: string
    - name: giturl
      type: string
    - name: revision
      type: string
    # ▼▼▼ [추가] EventListener에서 넘어오는 설정값 ▼▼▼
    - name: gradle-workers
      type: string
      default: "12"
    - name: gradle-memory
      type: string
      default: "10g"
  
  workspaces:
    - name: {{ .Values.project_name }}
      description: Not used, but kept for compatibility
  
  results:
    - name: image-url
      description: The built image URL
  
  steps:
    - name: create-kaniko-job
      image: "harbor.8ai.store/common/kubectl:1.34.2"
      script: |
        #!/bin/sh
        set -e
        
        JOB_NAME="kaniko-build-$(params.module-name)-$(params.timestamp)"
        
        MODULE_NAME="$(params.module-name)"
        
        if echo "${MODULE_NAME}" | grep -qE "(system|scheduler|message|portal|backoffice)"; then
          BASE_IMAGE="harbor.8ai.store/common/openjdk:17-jdk-oraclelinux8-otel-2.21.0"
          echo "✓ Module '${MODULE_NAME}' will use OpenTelemetry base image"
        else
          BASE_IMAGE="harbor.8ai.store/common/openjdk:17-jdk-oraclelinux8"
          echo "✓ Module '${MODULE_NAME}' will use standard base image"
        fi
        
        echo "Selected BASE_IMAGE: ${BASE_IMAGE}"
        echo ""

        cat <<EOF | kubectl apply --validate=false -f -
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: ${JOB_NAME}
          namespace: tekton-pipelines
          labels:
            tekton-trigger: "true"
            job-name: ${JOB_NAME}
            module: $(params.module-name)
        spec:
          ttlSecondsAfterFinished: 3600
          backoffLimit: 0
          template:
            metadata:
              labels:
                app: kaniko-build
                job-name: ${JOB_NAME}
                module: $(params.module-name)
            spec:
              nodeSelector:
                workload-type: tekton-build
              tolerations:
              - key: workload
                operator: Equal
                value: tekton-build
                effect: NoSchedule
              restartPolicy: Never
              serviceAccountName: tekton-ci-token
              containers:
              - name: kaniko-build
                image: harbor.8ai.store/common/kaniko-git:v1.23.2
                command:
                - /busybox/sh
                - -c
                - |
                  #!/bin/sh
                  set -e
                  
                  echo "=== Starting Build Process ==="
                  echo ""

                  echo "=== Git Credentials ==="
                  if [ -f /secrets/git-token/username ] && [ -f /secrets/git-token/password ]; then
                    GIT_USERNAME=\$(cat /secrets/git-token/username)
                    GIT_PASSWORD=\$(cat /secrets/git-token/password)
                    git config --global credential.helper store
                    mkdir -p /root
                    echo "https://\${GIT_USERNAME}:\${GIT_PASSWORD}@github.com" > /root/.git-credentials
                    chmod 600 /root/.git-credentials
                    echo "✓ Git credentials configured"
                  fi
                  echo ""
                  
                  echo "=== Git Clone ==="
                  git config --global user.name "Tekton CI"
                  git config --global user.email "ci@goyoai.com"
                  git config --global --add safe.directory /workspace
                  
                  if git clone -b $(params.revision) $(params.giturl) /workspace; then
                    cd /workspace
                    echo "✓ Clone successful (commit: \$(git rev-parse --short HEAD))"
                  else
                    echo "✗ Clone failed"
                    exit 1
                  fi
                  echo ""
                  
                  echo "=== DEBUG: Workspace Contents ==="
                  echo "Current directory: \$(pwd)"
                  echo ""
                  echo "Top-level files and directories:"
                  ls -la /workspace/
                  echo ""
                  
                  # ▼▼▼ [수정] Dockerfile 이름은 파라미터에서 직접 받습니다 ▼▼▼
                  DOCKERFILE_PATH="/workspace/$(params.dockerfile-name)"
                  echo "Checking expected path: \${DOCKERFILE_PATH}"
                  
                  if [ -f "\${DOCKERFILE_PATH}" ]; then
                    echo "✓ Dockerfile found at: \${DOCKERFILE_PATH}"
                    ls -lh "\${DOCKERFILE_PATH}"
                  else
                    echo "✗ Dockerfile NOT found at: \${DOCKERFILE_PATH}"
                    echo ""
                    echo "Searching for dockerfile in common locations..."
                    
                    for name in "$(params.dockerfile-name)" "Dockerfile.unified" "Dockerfile-system-dev" "DOCKERFILE-SYSTEM-DEV" "Dockerfile" "dockerfile"; do
                      for base in "/workspace" "/workspace/$(params.module-name)" "/workspace/docker"; do
                        test_path="\${base}/\${name}"
                        if [ -f "\${test_path}" ]; then
                          echo "✓ Found at: \${test_path}"
                          DOCKERFILE_PATH="\${test_path}"
                          ls -lh "\${DOCKERFILE_PATH}"
                          break 2
                        fi
                      done
                    done
                    
                    if [ ! -f "\${DOCKERFILE_PATH}" ]; then
                      echo ""
                      echo "Attempting wildcard search..."
                      FOUND_DOCKERFILE=\$(find /workspace -iname "*dockerfile*" -type f | head -1)
                      if [ -n "\${FOUND_DOCKERFILE}" ]; then
                        echo "✓ Found dockerfile via search: \${FOUND_DOCKERFILE}"
                        DOCKERFILE_PATH="\${FOUND_DOCKERFILE}"
                        ls -lh "\${DOCKERFILE_PATH}"
                      fi
                    fi
                  fi
                  
                  echo ""
                  echo "=== Final Dockerfile Path Check ==="
                  if [ -f "\${DOCKERFILE_PATH}" ]; then
                    echo "✓ Using Dockerfile: \${DOCKERFILE_PATH}"
                    echo "File size: \$(du -h \${DOCKERFILE_PATH} | cut -f1)"
                    echo "First 5 lines:"
                    head -5 "\${DOCKERFILE_PATH}"
                  else
                    echo "✗ CRITICAL: No valid Dockerfile found!"
                    echo "Cannot proceed with build."
                    exit 1
                  fi
                  echo ""
                  
                  echo "=== Pre-authenticating with Registry ==="
                  echo "Testing Harbor connection..."
                  if command -v curl > /dev/null 2>&1; then
                    HTTP_CODE=\$(curl -s -o /dev/null -w "%{http_code}" -u "\${REGISTRY_USER}:\${REGISTRY_PASS}" https://\${REGISTRY_URL}/v2/)
                    if [ "\${HTTP_CODE}" = "200" ]; then
                      echo "✓ Harbor authentication successful (HTTP \${HTTP_CODE})"
                    else
                      echo "⚠ Harbor authentication returned HTTP \${HTTP_CODE}"
                      echo "Continuing anyway..."
                    fi
                  fi
                  echo ""
                  
                  echo "=== Kaniko Build ==="
                  IMAGE_TAG="$(params.container-registry-url)/$(params.module-name):$(params.git-short-sha)-$(params.timestamp)"
                  echo "Image: \${IMAGE_TAG}"
                  echo "Dockerfile: \${DOCKERFILE_PATH}"
                  echo "Context: /workspace"
                  echo "Module: $(params.module-name)"
                  echo "Base Image: ${BASE_IMAGE}"
                  echo ""
                  
                  # Calculate relative path for Dockerfile from context
                  RELATIVE_DOCKERFILE=\$(echo "\${DOCKERFILE_PATH}" | sed 's|^/workspace/||' | sed 's|^|./|')
                  echo "Relative Dockerfile path: \${RELATIVE_DOCKERFILE}"
                  echo ""
                  
                  if [ ! -f "\${DOCKERFILE_PATH}" ]; then
                    echo "✗ CRITICAL: Dockerfile does not exist at \${DOCKERFILE_PATH}"
                    exit 1
                  fi
                  
                  cd /workspace
                  echo "Changed to /workspace directory"
                  echo "PWD: \$(pwd)"
                  echo ""
                  
                  echo "=== Module Configuration Selection ==="
                  echo "Module Name: $(params.module-name)"
                  echo ""

                  # ▼▼▼▼▼ [수정됨] 복잡한 CASE문 대신 파라미터를 변수에 할당 ▼▼▼▼▼
                  # EventListener에서 결정한 값을 그대로 사용합니다.
                  WORKERS_TO_USE="$(params.gradle-workers)"
                  GRADLE_MEM_TO_USE="$(params.gradle-memory)"
                  
                  # Kotlin 메모리는 파라미터에 없으므로, Kapt를 위해 넉넉한 기본값(145g)을 사용하거나
                  # 필요시 EventListener에 파라미터를 추가해야 합니다.
                  # 여기서는 안전하게 기존의 큰 값으로 고정합니다. (Unified일땐 무시됨)
                  KOTLIN_MEM_TO_USE="145g"
                  
                  echo "✓ Configuration Applied from Trigger:"
                  echo "  - Dockerfile: $(params.dockerfile-name)"
                  echo "  - Workers: \${WORKERS_TO_USE}"
                  echo "  - Memory: \${GRADLE_MEM_TO_USE}"
                  # ▲▲▲▲▲ [수정 완료] ▲▲▲▲▲

                  echo ""
                  echo "=== Final Configuration ==="
                  echo "Dockerfile Path: \${DOCKERFILE_PATH}"
                  echo "Relative Path: \${RELATIVE_DOCKERFILE}"
                  echo "Gradle Workers: \${WORKERS_TO_USE}"
                  echo "Gradle Memory: \${GRADLE_MEM_TO_USE}"
                  echo ""
                  
                  if [ ! -f "\${DOCKERFILE_PATH}" ]; then
                    echo "✗ CRITICAL: Dockerfile NOT found at: \${DOCKERFILE_PATH}"
                    echo ""
                    echo "Available Dockerfiles in /workspace:"
                    find /workspace -maxdepth 1 -iname "*dockerfile*" -type f 2>/dev/null || echo "None found"
                    exit 1
                  fi
                  
                  echo "✓ Dockerfile exists and ready to use"
                  echo "File size: \$(du -h \${DOCKERFILE_PATH} | cut -f1)"
                  echo ""
                  
                  if grep -q "ARG _G_MEM" "\${DOCKERFILE_PATH}"; then
                    echo "✓ Kapt Dockerfile detected. Using _G_MEM and _K_MEM."
                    BUILD_ARGS="--build-arg=MODULE_NAME=$(params.module-name) --build-arg=BASE_IMAGE=${BASE_IMAGE} --build-arg=GRADLE_WORKERS=\${WORKERS_TO_USE} --build-arg=_G_MEM=\${GRADLE_MEM_TO_USE} --build-arg=_K_MEM=\${KOTLIN_MEM_TO_USE}"
                  elif grep -q "ARG GRADLE_MAX_MEMORY" "\${DOCKERFILE_PATH}"; then
                    echo "✓ Standard Dockerfile detected. Using GRADLE_MAX_MEMORY."
                    BUILD_ARGS="--build-arg=MODULE_NAME=$(params.module-name) --build-arg=BASE_IMAGE=${BASE_IMAGE} --build-arg=GRADLE_WORKERS=\${WORKERS_TO_USE} --build-arg=GRADLE_MAX_MEMORY=\${GRADLE_MEM_TO_USE}"
                  else
                    echo "✓ Legacy Dockerfile detected (no memory args)."
                    BUILD_ARGS="--build-arg=MODULE_NAME=$(params.module-name) --build-arg=BASE_IMAGE=${BASE_IMAGE} --build-arg=GRADLE_WORKERS=\${WORKERS_TO_USE}"
                  fi
                  
                  echo "Build Args: \${BUILD_ARGS}"
                  echo ""    

                  /kaniko/executor \
                    --context=/workspace \
                    --dockerfile=\${RELATIVE_DOCKERFILE} \
                    --destination=\${IMAGE_TAG} \
                    \${BUILD_ARGS} \
                    --force \
                    --skip-tls-verify \
                    --skip-push-permission-check \
                    --skip-unused-stages \
                    --use-new-run \
                    \
                    --ignore-path=/var/spool/mail \
                    --ignore-path=/var/mail \
                    --ignore-path=/var/spool \
                    --ignore-path=/root/.kotlin \
                    --ignore-path=/tmp \
                    \
                    --cache=true \
                    --cache-repo=$(params.container-registry-cache-url)/backend-common \
                    --cache-copy-layers=true \
                    --cache-run-layers=true \
                    \
                    --snapshot-mode=time \
                    --ignore-var-run \
                    --cleanup \
                    --cache-dir=/cache \
                    --verbosity=info
                  
                  BUILD_EXIT=\$?
                  
                  if [ \${BUILD_EXIT} -ne 0 ]; then
                    echo ""
                    echo "✗ Build failed with exit code \${BUILD_EXIT}"
                    exit 1
                  fi
                  
                  echo ""
                  echo "✓ Build complete!"

                env:
                - name: GRADLE_USER_HOME
                  value: "/root/.gradle"
                - name: GRADLE_OPTS
                  value: "-Dorg.gradle.daemon=false"
                
                # ▼▼▼ [중요] 인증 관련 기존 설정 유지 ▼▼▼
                - name: DOCKER_CONFIG
                  value: "/kaniko/.docker"
                # ▲▲▲ [중요] ▲▲▲
                
                volumeMounts:
                - name: workspace
                  mountPath: /workspace
                - name: kaniko-cache
                  mountPath: /cache
                - name: git-credentials
                  mountPath: /secrets/git-token
                  readOnly: true
                - name: harbor-config
                  mountPath: /kaniko/.docker
                  readOnly: true
                
                resources:
                  requests:
                    memory: "50Gi"
                    cpu: "8000m"
                  limits:
                    memory: "50Gi"
                    cpu: "8000m"
                    
              volumes:
              - name: workspace
                emptyDir: {}
              - name: kaniko-cache
                emptyDir: {}
              - name: git-credentials
                secret:
                  secretName: git-token
              - name: harbor-config
                secret:
                  secretName: kaniko-secret
                  items:
                  - key: config.json
                    path: config.json
                    mode: 0644                              
        EOF
        
        echo "Job ${JOB_NAME} created successfully"
        echo "${JOB_NAME}" > /tmp/job-name
      volumeMounts:
        - name: job-info
          mountPath: /tmp
    
    - name: wait-for-job
      image: "harbor.8ai.store/common/kubectl:1.34.2"
      script: |
        #!/bin/sh
        set -e
        # ... (wait-for-job 로직은 변경 없음, 그대로 유지) ...
        JOB_NAME=$(cat /tmp/job-name)
        echo "=========================================="
        echo "📦 Waiting for Kaniko Build Job"
        echo "=========================================="
        echo "Job Name: ${JOB_NAME}"
        echo "Timeout: 1800 seconds (30 minutes)"
        echo "=========================================="
        echo ""
        
        echo "⏳ Waiting for Pod to be created..."
        POD_NAME=""
        for i in $(seq 1 60); do
          POD_NAME=$(kubectl get pods -n tekton-pipelines -l job-name=${JOB_NAME} -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
          if [ -n "$POD_NAME" ]; then
            echo "✓ Pod created: ${POD_NAME}"
            break
          fi
          sleep 1
        done
        
        if [ -z "$POD_NAME" ]; then
          echo "✗ Pod creation timeout"
          exit 1
        fi
        
        echo ""
        echo "⏳ Waiting for Pod to start running..."
        for i in $(seq 1 120); do
          POD_STATUS=$(kubectl get pod ${POD_NAME} -n tekton-pipelines -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
          if [ "$POD_STATUS" = "Running" ] || [ "$POD_STATUS" = "Succeeded" ]; then
            echo "✓ Pod is ${POD_STATUS}"
            break
          fi
          if [ $((i % 10)) -eq 0 ]; then
            echo "  Pod status: ${POD_STATUS} (${i}s)"
          fi
          sleep 1
        done
        
        echo ""
        echo "=========================================="
        echo "📋 Streaming Kaniko Build Logs"
        echo "=========================================="
        echo ""
        
        kubectl logs -f ${POD_NAME} -n tekton-pipelines -c kaniko-build 2>/dev/null &
        LOG_PID=$!
        
        TIMEOUT=1800
        ELAPSED=0
        INTERVAL=5
        
        while [ $ELAPSED -lt $TIMEOUT ]; do
          STATUS=$(kubectl get job ${JOB_NAME} -n tekton-pipelines -o jsonpath='{.status.conditions[?(@.type=="Complete")].status}' 2>/dev/null || echo "")
          FAILED=$(kubectl get job ${JOB_NAME} -n tekton-pipelines -o jsonpath='{.status.conditions[?(@.type=="Failed")].status}' 2>/dev/null || echo "")
          
          if [ "$STATUS" = "True" ]; then
            sleep 2
            kill $LOG_PID 2>/dev/null || true
            echo ""
            echo "=========================================="
            echo "✅ Build Completed Successfully!"
            echo "=========================================="
            echo "Image: $(params.container-registry-url)/$(params.module-name):$(params.git-short-sha)-$(params.timestamp)"
            echo "=========================================="
            echo "$(params.container-registry-url)/$(params.module-name):$(params.git-short-sha)-$(params.timestamp)" > $(results.image-url.path)
            exit 0
          fi
          
          if [ "$FAILED" = "True" ]; then
            kill $LOG_PID 2>/dev/null || true
            echo ""
            echo "=========================================="
            echo "❌ Build Failed"
            echo "=========================================="
            echo ""
            echo "=== Last 1000 lines of logs ==="
            kubectl logs ${POD_NAME} -n tekton-pipelines -c kaniko-build --tail=1000 2>/dev/null || true
            echo ""
            echo "=== Pod Status ==="
            kubectl describe pod ${POD_NAME} -n tekton-pipelines | tail -50
            exit 1
          fi
          
          sleep $INTERVAL
          ELAPSED=$((ELAPSED + INTERVAL))
        done
        
        kill $LOG_PID 2>/dev/null || true
        echo ""
        echo "=========================================="
        echo "⏱️  Build Timeout (30 minutes)"
        echo "=========================================="
        exit 1
      volumeMounts:
        - name: job-info
          mountPath: /tmp
  
  volumes:
    - name: job-info
      emptyDir: {}